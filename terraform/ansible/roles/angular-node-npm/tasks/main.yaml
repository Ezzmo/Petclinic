- name: install nodeJs
  become: true
  shell: sudo apt install nodejs-legacy

- name: install angular
  shell: npm install -g @angular/cli@latest
  become: true

- name: install Karma
  become: true
  shell: sudo npm install -g karma
